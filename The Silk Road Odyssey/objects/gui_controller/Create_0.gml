// Create Event

// Define the states (you can use constants or enum values)
enum state {
    idle,
	dialog,
    dialog_question,
    trading
    
}

// Initialize the object's initial state
currentState = state.idle;

